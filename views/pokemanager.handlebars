<nav class="navbar has-background-dark" role="navigation" aria-label="main navigation" id="top-nav">
  <div class="navbar-brand">
    <a class="navbar-item has-text-white"> 
        Pokemon Manager
      <img src="../images/pokeball.png">
    </a>
    <a id="burger-menu" role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false"
      data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>
  <div class="navbar-end">
    <a href="/Team Manager" class="navbar-item has-text-light is-size-4" id="NavLink"> 
    Team Manager
    </a>
    <a href="/" class="navbar-item has-text-light is-size-4" id="NavLink">
    Home Page
    </a>
  </div>
</nav>

<label class="container">
  <form>
    <label class="field">
      <label class="label">Pokemon Name or ID</label>
      <input type="text" class="input pokemonName" placeholder="Enter a Pokemon Name or ID">
    </label>

    <div class="control">
      <button class="button is-primary" id="addPokemon">Submit</button>
    </div>
  </form>
</label>
{{!-- Display searched Pokemon --}}
{{!-- <div class='columns'>
  {{#if pokesearch}}
  {{#each pokesearch}}
  {{> pokemonchoice}}
  {{/each}}
  {{/if}}
</div> --}}
{{!-- 
{{!-- Found out we can add scripts to these --}}
<script>
  $(document).ready(function(){
    $('form').on('submit', function(event){
      event.preventDefault();
      var name = $('input').val().toLowerCase();
      queryurl = 'https://pokeapi.co/api/v2/pokemon/' + name;
      $.get(queryurl, function(data){
        {{!-- Now we build the data into the page --}}
        
      })
    })
  })
</script> --}}
